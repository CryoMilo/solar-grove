version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: solar-grove-db
    environment:
      POSTGRES_USER: solargrove
      POSTGRES_PASSWORD: solargrove_dev_pass
      POSTGRES_DB: solargrove
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U solargrove -d solargrove']
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: solar-grove-redis
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
